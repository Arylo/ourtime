{
  "name": "ourtime",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "test:run": "turbo run test:run"
  },
  "devDependencies": {
    "shadcn": "^3.6.2",
    "turbo": "^2.3.3"
  }
}
